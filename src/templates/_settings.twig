{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Site Key',
    instructions: 'Get the site key on your dashboard',
    id: 'googleRecaptchaSiteKey',
    name: 'captchaSettings[googleRecaptchaSiteKey]',
    required: true,
    errors: null,
    value: settings['googleRecaptchaSiteKey'] ?? null
}) }}

{{ forms.textField({
    label: 'Secret key',
    instructions: 'Get the secret key on your dashboard',
    id: 'googleRecaptchaSecretKey',
    name: 'captchaSettings[googleRecaptchaSecretKey]',
    required: true,
    value: settings['googleRecaptchaSecretKey'] ?? null
}) }}